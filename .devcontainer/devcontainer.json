// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/python
{
	"name": "template_builder",
	"image": "mcr.microsoft.com/devcontainers/base:dev-debian",
	"postCreateCommand": "apt update && apt install -y wget && wget -q https://kcl-lang.io/script/install-cli.sh -O - | bash && wget https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64 -O /usr/bin/yq && chmod +x /usr/bin/yq",
	"remoteUser": "root",
	"workspaceMount": "source=${localWorkspaceFolder},target=/workspace/homelab-lxc,type=bind,consistency=cached,Z=true",
	"workspaceFolder": "/workspace/homelab-lxc",
	"mounts": [
	  "source=/run/user/1000/podman/podman.sock,target=/var/run/docker.sock,type=bind,consistency=cached"
	]
}
